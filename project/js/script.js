function checkAnswer() {
    let input = document.getElementById('answerInput').value // Получаем значение поля ввода
    if (input === '21') {
     // Проверяем, совпадает ли введенный текст с правильным ответом
     alert('Ваша координата 55.50673260101035, 37.12331338181866 !') // Если да, показываем сообщение об этом
    } else {
     alert('Ответ неверный. Попробуйте ещё раз!') // Если нет, показываем другое сообщение
    }
   }